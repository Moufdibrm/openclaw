# Memory Lifecycle Repair Report

Date: `2026-05-07`

Scope: local memory/KG repair and governance validation. No gateway, deploy, service restart, Mission Manager code, or production runtime wiring was changed.

## Repair Summary

The local durable KG corruption is repaired.

Canonical KG paths are now aligned byte-for-byte:

- `/Users/moufdi/.openclaw/knowledge-graph/memory.jsonl`
- `/Users/moufdi/.clawdis/knowledge-graph/memory.jsonl`

Current repaired KG:

- entries: `181`
- entities: `108`
- relations: `73`
- SHA-256: `8b9f891b8ce386ec056b559586bead93f952d9760e58a17776aca4da43318eb1`
- strict shared-graph validation: `0` errors, `0` warnings

Repair artifacts:

- `/Users/moufdi/.openclaw/knowledge-graph/repair-20260506T232721Z/`

Pre-repair backups:

- `/Users/moufdi/.openclaw/knowledge-graph/memory.jsonl.backup.pre-memory-lifecycle-repair.20260506T232721Z`
- `/Users/moufdi/.clawdis/knowledge-graph/memory.jsonl.backup.pre-memory-lifecycle-repair.20260506T232721Z`

Intermediate registry-migration backups:

- `/Users/moufdi/.openclaw/knowledge-graph/memory.jsonl.backup.intermediate-registry-migration.20260506T232907Z`
- `/Users/moufdi/.clawdis/knowledge-graph/memory.jsonl.backup.intermediate-registry-migration.20260506T232907Z`

## What Changed

The repair preserved:

- the valid historical KG v2 backup entries from `memory.jsonl.backup.kg-v2.20260422-220928`
- the registry/channel baseline generated by the KG v2 migration
- the `.openclaw` and `.clawdis` active files in sync

The truncated invalid v1 line was not reintroduced:

- `24Seven Avant Alpha Audit 2026-04-16 Results`
- reason: source line is incomplete JSON and the invalid-line marker is the same truncated content.

## Validation

Shared graph strict validation:

- `.openclaw`: `entry_count=181`, `error_count=0`, `warning_count=0`
- `.clawdis`: `entry_count=181`, `error_count=0`, `warning_count=0`

KG CLI validation:

- returns `OK`
- warnings remain about legacy observation formatting and skill-side entity type allowlist drift
- these are not shared-graph blocking errors

Jack-X candidate no-apply proof:

- candidate: `/Users/moufdi/.openclaw/workspace-jack-x/runtime/graph-candidates/cand-jack-x-lark-table-5483e581ee7f.json`
- command mode: no `--apply`
- result: valid candidate, `18` entity results, `28` relation results
- KG hash unchanged after no-apply proof.

Promotion review proof:

- mode: deterministic
- result: `21` candidates reviewed
- all `21` route to `review`
- no automatic promotion was performed.

Memory projection after repair:

- output: `/Users/moufdi/.openclaw/knowledge-graph/repair-20260506T232721Z/memory-projection-after-repair.json`
- KG summary: `181` entries, `108` entities, `73` relations
- relation types: `belongs_to`, `member_of`, `uses`
- channel coverage: `32` registered channels, `13` covered channels, `0` pending analysis channels, `0` unregistered channels.

LLM Wiki active compile:

- latest manifest: `/Users/moufdi/.openclaw/memory-wiki/latest.json`
- run id: `memwiki_20260506T233525Z_1c3d1c21`
- source reports: `173`
- selected channels: `9`
- pages: `40`
- KG operations: `40`
- review items: `37`
- semantic mode: `active`
- provider: `gemini`
- semantic calls: `38` fresh, `2` cache hits, `0` failures

## Current State

Resolved:

- canonical KG JSONL corruption
- `.openclaw` and `.clawdis` active file mismatch risk for current content
- LLM Wiki stale local run; latest wiki now reflects May 2026 Jack-X reports
- SSL issue for local Gemini calls by running with the local `certifi` CA bundle

Still open:

- `.clawdis` still has independent tool/MCP pointers; it is aligned now, but not structurally aliased to `.openclaw`
- Jack-X DB analysis commits are not gated by the promotion review result; the review is recorded, then the full graph candidate is still committed when `--apply` runs
- LLM Wiki emits `kg_operations`, but there is no closed handoff into Jack-X refinement review
- Memory Wiki pages still do not render KG relation context such as `Najet member_of Team :: BRM Board`, even when the projection contains the relation
- the future-time feedback loop is not yet route-backed: there is no repeatable proof for previous fact -> later signal -> correction candidate -> review -> superseded/confirmed KG update -> retrieval
- no live KG commit replay was run after repair; only no-apply and staging proofs were run

## Next Required Slice

1. Add a review gate before Jack-X `commit-candidate --apply`.
2. Add the `LLM Wiki kg_operations -> Jack-X refinement review` bridge.
3. Teach Memory Wiki page seeds/pages to include bounded KG relation evidence from memory projection.
4. Add Jack-X feedback-loop route for correction/staleness/contradiction review over time.
5. Convert `.clawdis/knowledge-graph` into a compatibility alias or update its tool/MCP pointers to `.openclaw`.
6. Run one low-risk live KG commit under supervised mode after the review gate exists.
7. Prove one autocorrection fixture with history-preserving metadata.
